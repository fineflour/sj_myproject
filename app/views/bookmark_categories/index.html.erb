<h1>My Bookmark Category</h1>

<div class="row">
    <div class="col-md-8">
      <% @bookmark_categories.each do |topic| %>
          <div class="media">
          <div class="media-body">
            <h4 class="media-heading">
              <%= link_to bookmark_categories.name, topic %>
            </h4>
            <small>
            </small>
          </div>
        </div>
      <% end %>
    </div>
    <div class="col-md-4">
    </div>
  </div>
